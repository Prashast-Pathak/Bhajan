# Claude Prompt: Shlokas JSON Writer

Task: Fill `templates/pillar-json/shlokas.template.json` format.

Writing style:
- Scholarly but simple.
- Keep Sanskrit exact and clean.
- Keep commentary practical and contextual.

Mandatory checks:
- Include `word_by_word` with meaningful terms.
- Keep `source` and `source_ref` precise.
- No mystical exaggeration.

Output: valid JSON array only.
